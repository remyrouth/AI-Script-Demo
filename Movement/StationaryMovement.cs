using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StationaryMovement : AIMovement
{
    // Start is called before the first frame update
    void Start()
    {
        base.InitializeComponents();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
